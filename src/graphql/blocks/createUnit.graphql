mutation blocksCreateUnit(
  $unit: UnitInput!
  $position: PositionInput!
  $controlTeamId: ID!
) {
  blocksCreateUnit(
    unit: $unit
    position: $position
    controlTeamId: $controlTeamId
  ) {
    status
    errors {
      message
    }
  }
}
