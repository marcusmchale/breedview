#import "./unitFragment.graphql"
query units (
    $unitIds: [ID!]
){
    blocksUnits(
        unitIds: $unitIds
    ){
        status
        result {
            ...UnitFields
        }
        errors {
            message
        }
    }
}