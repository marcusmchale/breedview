#import "./unitFragment.graphql"
query units (
    $unitIds: [ID!]
){
    blocksUnits(
        ids: $unitIds
    ){
        status
        result {
            ...UnitFields
        }
        errors {
            message
        }
    }
}