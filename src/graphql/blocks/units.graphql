#import "./unitFragment.graphql"
query units (
    $unitIds: [Int!]
){
    blocksUnits(
        unitIds: $unitIds
    ){
        status
        result {
            ...UnitFields
        }
        errors {
            message
        }
    }
}