#import "./unitFragment.graphql"
query blocks ($locationIds: [ID!]!) {
    blocks (locationIds: $locationIds) {
        status
        result {
            ...UnitFields
        }
        errors {
            message
        }
    }
}