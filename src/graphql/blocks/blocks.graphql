#import "./unitFragment.graphql"
query blocks ($locationIds: [Int!]!) {
    blocks (locationIds: $locationIds) {
        status
        result {
            ...UnitFields
        }
        errors {
            message
        }
    }
}