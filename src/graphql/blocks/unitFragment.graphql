fragment UnitFields on Unit {
    id
    subject { id versionId view name }
    germplasm { id name }
    name
    description
    positions {
        location { id name }
        layout { id name axes type { id versionId view name } }
        coordinates
        start
        end
    }
    parents { id }
    children { id }
}