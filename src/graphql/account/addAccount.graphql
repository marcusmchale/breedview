mutation addAccount(
    $name: String!,
    $fullname: String!,
    $email: String!,
    $password: String!
) {
    add_account(
        name: $name,
        fullname: $fullname,
        email: $email,
        password: $password
    ) {
        status
        errors {
            message
        }
    }
}