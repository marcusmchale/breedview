mutation accountsSetOntologyRole(
    $userId: ID!,
    $ontologyRole: OntologyRole!
) {
    accountsSetOntologyRole(
        userId: $userId,
        ontologyRole: $ontologyRole
    ) {
        status
        errors {
            message
        }
    }
}