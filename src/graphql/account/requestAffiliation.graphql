mutation accountsRequestAffiliation($team: ID!, $access: Access!, $heritable: Boolean) {
  accountsRequestAffiliation(teamId: $team, access: $access, heritable: $heritable) {
    status
    errors {
      message
    }
  }
}
