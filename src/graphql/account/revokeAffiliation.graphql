mutation accountsRevokeAffiliation($user: Int!, $team: Int!, $access: Access!) {
  accountsRevokeAffiliation(userId: $user, teamId: $team, access: $access) {
    status
    errors {
      message
    }
  }
}
