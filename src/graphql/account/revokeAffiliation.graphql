mutation accountsRevokeAffiliation($user: ID!, $team: ID!, $access: Access!) {
  accountsRevokeAffiliation(userId: $user, teamId: $team, access: $access) {
    status
    errors {
      message
    }
  }
}
