query accountsAccount {
    accountsAccount{
        status
        result {
            user {id name fullname email ontologyRole, ontologyRoleRequested}
            allowedEmails
        }
        errors {
            message
        }
    }
}