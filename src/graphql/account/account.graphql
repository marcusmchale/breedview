query accountsAccount {
    accountsAccount{
        status
        result {
            user {id name fullname email}
            allowedEmails
        }
        errors {
            message
        }
    }
}