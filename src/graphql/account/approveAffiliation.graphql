mutation accountsApproveAffiliation($user: ID!, $team: ID!, $access: Access!, $heritable: Boolean) {
  accountsApproveAffiliation(userId: $user, teamId: $team, access: $access, heritable: $heritable) {
    status
    errors {
      message
    }
  }
}
