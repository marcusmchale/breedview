mutation approveAffiliation($user: Int!, $team: Int!, $access: Access!, $heritable: Boolean) {
  approve_affiliation(user: $user, team: $team, access: $access, heritable: $heritable) {
    status
    errors {
      message
    }
  }
}
