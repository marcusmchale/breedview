mutation accountsUpdateUser(
    $name: String,
    $fullname: String,
    $email: String,
    $password: String
) {
    accountsUpdateUser(
        name: $name,
        fullname: $fullname,
        email: $email,
        password: $password
    ) {
        status
        errors {
            message
        }
    }
}