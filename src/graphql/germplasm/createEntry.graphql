mutation germplasmCreateEntry(
    $entry: GermplasmInput!
    $controlTeamId: ID!
    $release: ReadRelease
) {
    germplasmCreateEntry(
        entry: $entry
        controlTeamId: $controlTeamId
        release: $release
    ) {
        status
        errors {
            message
        }
    }
}