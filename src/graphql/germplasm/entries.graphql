#import "./entryFragment.graphql"
query germplasmEntries(
    $entryIds: [ID!]
    $names: [String!]
) {
    germplasmEntries(
        entryIds: $entryIds
        names: $names
    ){
        status
        result {
            ...GermplasmEntryFields
        }
        errors {
            message
        }
    }
}