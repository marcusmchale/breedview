#import "./entryFragment.graphql"
query germplasmEntries(
    $entryIds: [ID!]
    $names: [String!]
) {
    germplasmEntries(
        ids: $entryIds
        names: $names
    ){
        status
        result {
            ...GermplasmEntryFields
        }
        errors {
            message
        }
    }
}