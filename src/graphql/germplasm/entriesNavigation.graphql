#import "./entryFragmentNavigation.graphql"
query germplasmEntriesNavigation(
    $entryIds: [ID!]
) {
    germplasmEntries(
        ids: $entryIds
    ){
        status
        result {
            ...GermplasmNavigationFields
        }
        errors {
            message
        }
    }
}