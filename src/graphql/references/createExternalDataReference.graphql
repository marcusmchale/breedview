mutation referencesCreateExternalData(
  $reference: CreateExternalDataReferenceInput!
  $controlTeamId: ID!
) {
  referencesCreateExternalData(
    reference: $reference
    controlTeamId: $controlTeamId
  ) {
    status
    result
    errors {
      message
    }
  }
}
