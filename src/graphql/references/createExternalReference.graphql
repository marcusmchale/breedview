mutation referencesCreateExternal(
  $reference: ExternalReferenceInput!
  $controlTeamId: ID!
  $release: ReadRelease
) {
  referencesCreateExternal(
    reference: $reference
    controlTeamId: $controlTeamId
    release: $release
  ) {
    status
    result
    errors {
      message
    }
  }
}
