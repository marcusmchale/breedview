query referencesFileDownloadState($fileId: ID!) {
  referencesFileDownloadState(fileId: $fileId) {
    status
    result {
      status
      progress {
        startedAt
        progress
      }
    }
    errors {
      message
    }
  }
}
