mutation referencesCreateDataFile(
  $reference: DataFileReferenceInput!
  $controlTeamId: ID!
  $release: ReadRelease
) {
  referencesCreateDataFile(
    reference: $reference
    controlTeamId: $controlTeamId
    release: $release
  ) {
    status
    result
    errors {
      message
    }
  }
}
