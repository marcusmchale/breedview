mutation referencesCreateFile(
  $reference: CreateFileReferenceInput!
  $controlTeamId: ID!
) {
  referencesCreateFile(
    reference: $reference
    controlTeamId: $controlTeamId
  ) {
    status
    result
    errors {
      message
    }
  }
}
