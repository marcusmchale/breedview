mutation referencesCreateLegal(
  $reference: LegalReferenceInput!
  $controlTeamId: ID!
  $release: ReadRelease
) {
  referencesCreateLegal(
    reference: $reference
    controlTeamId: $controlTeamId
    release: $release
  ) {
    status
    result
    errors {
      message
    }
  }
}
