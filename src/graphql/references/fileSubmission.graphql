query referencesFileSubmission($fileId: ID!) {
  referencesFileSubmission(fileId: $fileId) {
    status
    result {
      referenceId
      status
      progress
      errors
    }
    errors {
      name
      message
    }
  }
}