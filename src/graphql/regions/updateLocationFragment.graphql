fragment LocationUpdate on Location {
    id
    name
    code
    type { id name versionId draft}
    description
    address
    coordinates { latitude longitude }
    children { id }
    parent { id }
}