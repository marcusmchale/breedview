mutation regionsCreateLocation(
  $location: LocationInput!
  $controlTeamId: ID!
) {
  regionsCreateLocation(
    location: $location
    controlTeamId: $controlTeamId
  ) {
    status
    errors {
      message
    }
  }
}
