query regionsCountries {
    regionsCountries {
        status
        result {
            id
            name
            code
            type {id}
        }
        errors {
            message
        }
    }
}