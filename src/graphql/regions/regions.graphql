query regions {
    regions {
        status
        result {
            id
            name
            code
            type {id, name}
            description
            address
            coordinates {latitude, longitude}
            children {id}
            parent {id}
        }
        errors {
            message
        }
    }
}