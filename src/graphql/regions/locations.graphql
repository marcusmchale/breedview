#import "./locationFragment.graphql"
query locations (
    $locationIds: [ID!]!
){
    regionsLocations(
        locationIds: $locationIds
    ){
        status
        result {
            ...LocationFields
        }
        errors {
            message
        }
    }
}