#import "./locationFragment.graphql"
query locations (
    $locationIds: [ID!]!
){
    regionsLocations(
        ids: $locationIds
    ){
        status
        result {
            ...LocationFields
        }
        errors {
            message
        }
    }
}