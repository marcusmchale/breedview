#import "./locationFragment.graphql"
query locations (
    $locationIds: [Int!]!
){
    regionsLocations(
        locationIds: $locationIds
    ){
        status
        result {
            ...LocationFields
        }
        errors {
            message
        }
    }
}