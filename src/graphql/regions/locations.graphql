query locations (
    $locationTypeId: Int,
    $locationIds: [Int!]
){
    regionsLocations(
        locationTypeId: $locationTypeId,
        locationIds: $locationIds
    ){
        status
        result {
            id
            name
            code
            type {id, name}
            description
            address
            coordinates {latitude, longitude}
            parent {id}
            children {id}
        }
        errors {
            message
        }
    }
}