fragment LocationFields on Location {
    id
    name
    code
    type { id versionId view name }
    description
    address
    coordinates { latitude longitude }
    region { id name }
    children { id name }
    parent { id name }
}

