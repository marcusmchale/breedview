fragment LocationFields on Location {
    id
    name
    code
    type { id, name }
    description
    address
    coordinates { latitude longitude }
    region { id, name }
    children { id, name }
    parent { id, name }
}

