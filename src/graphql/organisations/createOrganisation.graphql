mutation organisationsCreateTeam(
    $name: String!,
    $fullname: String
) {
    organisationsCreateTeam(
        name: $name,
        fullname: $fullname
    ) {
        status
        errors {
            message
        }
    }
}