mutation addTeam(
    $name: String!,
    $fullname: String
) {
    add_team(
        name: $name,
        fullname: $fullname
    ) {
        status
        errors {
            message
        }
    }
}