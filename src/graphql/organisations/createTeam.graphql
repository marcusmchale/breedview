mutation organisationsCreateTeam(
    $name: String!,
    $fullname: String,
    $parent: Int
) {
    organisationsCreateTeam(
        name: $name,
        fullname: $fullname,
        parentId: $parent
    ) {
        status
        errors {
            message
        }
    }
}