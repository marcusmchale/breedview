mutation CreateDataset(
  $dataset: DatasetInput!
  $controlTeamId: ID!
) {
  datasetsCreate(
    dataset: $dataset
    controlTeamId: $controlTeamId
  ) {
    status
    result
    errors {
      name
      message
    }
  }
}
