query DatasetSubmission($submissionId: ID!) {
  datasetsSubmission(submissionId: $submissionId) {
    status
    result {
      datasetId
      status
      errors
      itemErrors {
        index
        error
      }
    }
    errors {
      name
      message
    }
  }
}