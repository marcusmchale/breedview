query DatasetSubmission($submissionId: String!) {
  datasetsSubmission(submissionId: $submissionId) {
    status
    result {
      datasetId
      status
      errors
      itemErrors {
        index
        error
      }
    }
    errors {
      name
      message
    }
  }
}