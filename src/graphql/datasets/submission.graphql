query DatasetSubmission($submissionId: ID!) {
  datasetsSubmission(id: $submissionId) {
    status
    result {
      datasetId
      status
      errors
      itemErrors {
        index
        error
      }
    }
    errors {
      name
      message
    }
  }
}