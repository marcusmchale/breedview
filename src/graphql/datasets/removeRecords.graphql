mutation RemoveRecords($datasetId: ID!, $recordIds: [ID!]!) {
  datasetsRemoveRecords(datasetId: $datasetId, recordIds: $recordIds) {
    status
    result
    errors {
      name
      message
    }
  }
}