mutation RemoveRecords($datasetId: Int!, $recordIds: [Int!]!) {
  datasetsRemoveRecords(datasetId: $datasetId, recordIds: $recordIds) {
    status
    result
    errors {
      name
      message
    }
  }
}