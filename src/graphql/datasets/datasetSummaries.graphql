query DatasetsSummaries($studyId: ID!){
  datasetsSummaries(studyId: $studyId) {
    status
    result {
        id
        concept { id versionId view name }
        subjects { id versionId view name }
        locations { id name }
        blocks { id name }
        unit_count
        record_count
        start
        end
    }
    errors {
      name
      message
    }
  }
}