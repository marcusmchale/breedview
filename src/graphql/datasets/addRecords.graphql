mutation AddRecords($datasetId: Int!, $records: [RecordInput]) {
  datasetsAddRecords(datasetId: $datasetId, records: $records) {
    status
    result
    errors {
      name
      message
    }
  }
}