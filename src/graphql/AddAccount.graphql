mutation (
    $username: String!,
    $fullname: String!,
    $email: String!,
    $team_name: String!
    $password: String!
) { add_account(
    username: $username ,
    fullname: $fullname,
    email: $email,
    team_name: $team_name,
    password: $password
)}