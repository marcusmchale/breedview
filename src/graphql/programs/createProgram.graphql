mutation programsCreateProgram(
  $program: ProgramInput!
  $controlTeamId: ID!
) {
  programsCreateProgram(
    program: $program
    controlTeamId: $controlTeamId
  ) {
    status
    errors {
      message
    }
  }
}
