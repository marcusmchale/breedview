mutation programsCreateTrial(
  $trial: CreateTrialInput!
  $controlTeamId: ID!
) {
  programsCreateTrial(
    trial: $trial
    controlTeamId: $controlTeamId
  ) {
    status
    errors {
      message
    }
  }
}
