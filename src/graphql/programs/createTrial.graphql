mutation programsCreateTrial(
  $trial: TrialInput!
  $controlTeamId: ID!
) {
  programsCreateTrial(
    trial: $trial
    controlTeamId: $controlTeamId
  ) {
    status
    errors {
      message
    }
  }
}
