#import "../references/referenceFragment.graphql"
query programs {
  programs {
    status
    result {
      id
      name
      fullname
      description
      contacts { id }
      references { ...ReferenceFields }
      trials { id, name }
    }
    errors {
      message
    }
  }
}
