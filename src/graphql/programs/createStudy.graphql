mutation programsCreateStudy(
  $study: StudyInput!
  $controlTeamId: ID!
  $release: ReadRelease
) {
  programsCreateStudy(
    study: $study
    controlTeamId: $controlTeamId
    release: $release
  ) {
    status
    errors {
      message
    }
  }
}
