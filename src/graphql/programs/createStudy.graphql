mutation programsCreateStudy(
  $study: CreateStudyInput!
  $controlTeamId: ID!
) {
  programsCreateStudy(
    study: $study
    controlTeamId: $controlTeamId
  ) {
    status
    errors {
      message
    }
  }
}
