mutation programsCreateStudy(
  $study: StudyInput!
  $controlTeamId: ID!
) {
  programsCreateStudy(
    study: $study
    controlTeamId: $controlTeamId
  ) {
    status
    errors {
      message
    }
  }
}
