#import "../references/referenceFragment.graphql"
query programsStudy($studyId: ID!) {
  programsStudy(id: $studyId) {
    status
    result {
      id
      name
      fullname
      description
      practices
      start
      end
      design { id versionId view name }
      licence { id description text }
      references { ...ReferenceFields }
    }
    errors {
      message
    }
  }
}
