#import "../references/referenceFragment.graphql"
query programsProgram($programId: ID, $trialId: ID, $studyId: ID) {
  programsProgram(programId: $programId, trialId: $trialId, studyId: $studyId) {
    status
    result {
      id
      name
      fullname
      description
      contacts { id }
      references { ...ReferenceFields }
      trials { id name }
    }
    errors {
      message
    }
  }
}
