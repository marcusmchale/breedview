mutation controlsSetRelease(
    $entityLabel: ControlledModelLabel!
    $entityIds: [ID!]!
    $release: ReadRelease!
) {
  controlsSetRelease(entityLabel: $entityLabel, entityIds: $entityIds, release: $release) {
    status
    errors {
      message
    }
  }
}