#import "./layoutFragment.graphql"
query arrangements ($locationId: ID) {
    arrangements (locationId: $locationId) {
        status
        result {
            ...LayoutFields
        }
        errors {
            message
        }
    }
}