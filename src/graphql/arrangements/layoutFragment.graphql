fragment LayoutFields on Layout {
    id
    name
    axes
    type { id versionId view name }
    location { id name }
    arrangement { id name }
    parent { id name }
    children { id name }
    position
}