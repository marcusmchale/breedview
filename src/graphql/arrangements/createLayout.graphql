mutation arrangementsCreateLayout(
  $layout: LayoutInput!
  $controlTeamId: ID!
) {
  arrangementsCreateLayout(
    layout: $layout
    controlTeamId: $controlTeamId
  ) {
    status
    errors {
      message
    }
  }
}
