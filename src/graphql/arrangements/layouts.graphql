#import "./layoutFragment.graphql"
query layouts (
    $layoutIds: [ID!]
){
    arrangementsLayouts(
        ids: $layoutIds
    ){
        status
        result {
            ...LayoutFields
        }
        errors {
            message
        }
    }
}