#import "./layoutFragment.graphql"
query layouts (
    $layoutIds: [ID!]
){
    arrangementsLayouts(
        layoutIds: $layoutIds
    ){
        status
        result {
            ...LayoutFields
        }
        errors {
            message
        }
    }
}