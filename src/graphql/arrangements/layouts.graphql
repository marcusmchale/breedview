#import "./layoutFragment.graphql"
query layouts (
    $layoutIds: [Int!]
){
    arrangementsLayouts(
        layoutIds: $layoutIds
    ){
        status
        result {
            ...LayoutFields
        }
        errors {
            message
        }
    }
}