mutation accountsResetPassword($token: String!, $password: String!) {
    accountsResetPassword(token: $token, password: $password) {
        status
        errors {
            message
        }
    }
}