mutation ontologyCreateCategory(
    $category: CategoryInput!
) {
    ontologyCreateCategory(
        category: $category,
    ) {
        status
        errors {
            message
        }
    }
}