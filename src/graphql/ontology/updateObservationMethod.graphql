mutation ontologyUpdateObservationMethod(
    $observationMethod: ObservationMethodUpdate!
) {
    ontologyUpdateObservationMethod(
        observationMethod: $observationMethod,
    ) {
        status
        errors {
            message
        }
    }
}