mutation ontologyUpdateLocationType(
    $locationType: LocationTypeUpdate!
) {
    ontologyUpdateLocationType(
        locationType: $locationType,
    ) {
        status
        errors {
            message
        }
    }
}