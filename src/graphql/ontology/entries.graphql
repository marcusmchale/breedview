#import "./entryFragment.graphql"
query ontologyEntries(
    $entryIds: [ID!]
    $labels: [OntologyEntryLabel!]
    $versionId: ID
) {
    ontologyEntries(
        entryIds: $entryIds
        labels: $labels
        versionId: $versionId
    ){
        status
        result {
            ...OntologyEntryFields
        }
        errors {
            message
        }
    }
}