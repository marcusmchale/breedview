mutation ontologyUpdateCondition(
    $condition: ConditionUpdate!
) {
    ontologyUpdateCondition(
        condition: $condition,
    ) {
        status
        errors {
            message
        }
    }
}