mutation ontologyUpdateCategory(
    $category: CategoryUpdate!
) {
    ontologyUpdateCategory(
        category: $category,
    ) {
        status
        errors {
            message
        }
    }
}