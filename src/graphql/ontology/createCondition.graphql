mutation ontologyCreateCondition(
    $condition: ConditionInput!
) {
    ontologyCreateCondition(
        condition: $condition,
    ) {
        status
        errors {
            message
        }
    }
}