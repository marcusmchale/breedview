mutation ontologyCreateLocationType(
    $locationType: LocationTypeInput!
) {
    ontologyCreateLocationType(
        locationType: $locationType,
    ) {
        status
        errors {
            message
        }
    }
}