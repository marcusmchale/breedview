mutation ontologyCreateLayoutType(
    $layoutType: LayoutTypeInput!
) {
    ontologyCreateLayoutType(
        layoutType: $layoutType,
    ) {
        status
        errors {
            message
        }
    }
}