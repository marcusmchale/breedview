mutation ontologyCreateObservationMethod(
    $observationMethod: ObservationMethodInput!
) {
    ontologyCreateObservationMethod(
        observationMethod: $observationMethod,
    ) {
        status
        errors {
            message
        }
    }
}