mutation ontologyCreateControlMethod(
    $controlMethod: ControlMethodInput!
) {
    ontologyCreateControlMethod(
        controlMethod: $controlMethod,
    ) {
        status
        errors {
            message
        }
    }
}