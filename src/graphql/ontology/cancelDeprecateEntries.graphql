mutation ontologyCancelDeprecateEntries(
    $entryIds: [ID!]!
) {
    ontologyCancelDeprecateEntries(
        ids: $entryIds,
    ) {
        status
        errors {
            message
        }
    }
}
