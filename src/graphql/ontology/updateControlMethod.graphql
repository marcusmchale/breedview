mutation ontologyUpdateControlMethod(
    $controlMethod: ControlMethodUpdate!
) {
    ontologyUpdateControlMethod(
        controlMethod: $controlMethod,
    ) {
        status
        errors {
            message
        }
    }
}