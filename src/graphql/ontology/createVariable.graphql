mutation ontologyCreateVariable(
    $variable: VariableInput!
) {
    ontologyCreateVariable(
        variable: $variable,
    ) {
        status
        errors {
            message
        }
    }
}