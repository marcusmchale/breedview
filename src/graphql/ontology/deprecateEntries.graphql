mutation ontologyDeprecateEntries(
    $entry_ids: [Int!]
) {
    ontologyDeprecateEntries(
        entryIds: $entry_ids,
    ) {
        status
        errors {
            message
        }
    }
}
