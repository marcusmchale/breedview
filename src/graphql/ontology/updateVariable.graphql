mutation ontologyUpdateVariable(
    $variable: VariableUpdate!
) {
    ontologyUpdateVariable(
        variable: $variable,
    ) {
        status
        errors {
            message
        }
    }
}