query ontologyCommitHistory(
    $limit: Int
) {
    ontologyCommitHistory(
        limit: $limit
    ){
        status
        result {
            version {major, minor, patch, id}
            time
            comment
        }
        errors {
            message
        }
    }
}