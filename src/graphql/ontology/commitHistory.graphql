query ontologyCommitHistory(
    $limit: Int
    $lastVersionId: ID
) {
    ontologyCommitHistory(
        limit: $limit
        lastVersionId: $lastVersionId
    ){
        status
        result {
            version { major minor patch id }
            time
            comment
            licence {
                id, description, text
            }
            copyright {
                id, description, text
            }
        }
        errors {
            message
        }
    }
}